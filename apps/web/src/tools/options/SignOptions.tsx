// Sign & Fill uses its own workspace (SignWorkspace) instead of the generic
// ToolPage, so this panel is never actually rendered -- it only exists to
// satisfy ToolConfig's shape.
export function SignOptions() {
  return null;
}
